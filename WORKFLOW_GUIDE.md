# 📋 مسار العملية المحاسبية الكامل في النظام

## 🔄 المسار الشامل من البداية للنهاية

### 1️⃣ دخول المركبة للورشة
**الخطوة الأولى: تسجيل المركبة**

📍 **الصفحة**: `Dashboard` > زر "إضافة مركبة جديدة"
- يتم تسجيل بيانات المركبة:
  - بيانات المركبة (العلامة، الموديل، السنة، رقم اللوحة)
  - بيانات العميل (الاسم، الهاتف، البريد)
  - تاريخ الدخول
  - الحالة الأولية: `diagnosis` (تشخيص)

📊 **قاعدة البيانات**: 
```
Table: vehicles
{
  id, plateNumber, brand, model, year,
  customerName, customerPhone, customerEmail,
  entryDate, status, parts: []
}
```

⚠️ **لا يوجد قيد محاسبي في هذه المرحلة**

---

### 2️⃣ تشخيص المركبة وإضافة الخدمات/القطع
**الخطوة الثانية: إضافة بنود الإصلاح**

📍 **الصفحة**: `VehicleDetails` > إضافة بند
- يتم إضافة البنود للمركبة:
  ```javascript
  {
    itemType: 'service' أو 'part',
    name: 'اسم الخدمة/القطعة',
    quantity: 1,
    price: 500
  }
  ```

📊 **قاعدة البيانات**:
```
vehicles.parts = [
  { itemType: 'service', name: 'تغيير زيت', quantity: 1, price: 150 },
  { itemType: 'part', name: 'فلتر زيت', quantity: 2, price: 50 }
]
```

💰 **الحساب التلقائي**:
```python
estimatedTotal = sum(price × quantity لكل بند)
# مثال: 150 + (50 × 2) = 250 ريال
```

⚠️ **لا يوجد قيد محاسبي في هذه المرحلة أيضاً**

---

### 3️⃣ إنشاء عملية بيع (Sale Operation)
**الخطوة الثالثة: تسجيل عملية البيع**

📍 **الصفحة**: `Operations` > زر "عملية جديدة"
- نوع العملية: `sale` (بيع خدمة/قطع)
- اختيار المركبة
- إضافة البنود:
  ```javascript
  items: [
    { itemType: 'service', name: 'تغيير زيت', quantity: 1, price: 150 },
    { itemType: 'part', name: 'فلتر زيت', quantity: 2, price: 50 }
  ]
  ```
- اختيار طريقة الدفع: `cash` (نقدي) أو `credit` (آجل)
- العميل: اسم العميل

📊 **قاعدة البيانات**:
```
Table: operations
{
  id,
  type: 'sale',
  vehicle_id,
  partner_name: 'اسم العميل',
  items: [...],
  total: 250,
  payment_method: 'cash',
  op_date: '2025-01-25'
}
```

---

### 4️⃣ القيد المحاسبي التلقائي (Journal Entry)
**الخطوة الرابعة: تسجيل القيد المحاسبي تلقائياً**

💫 **القيد التلقائي عند حفظ عملية البيع (نقدي)**:

```
التاريخ: 2025-01-25
الوصف: بيع خدمات - تويوتا كامري ABC1234

┌────────────────────┬────────┬────────┐
│ الحساب            │ مدين   │ دائن   │
├────────────────────┼────────┼────────┤
│ 101 - النقدية     │ 250    │   -    │
│ 411 - إيرادات     │   -    │ 250    │
└────────────────────┴────────┴────────┘
الإجمالي:              250      250  ✅
```

**إذا كانت العملية آجلة (`credit`)**:
```
┌────────────────────────┬────────┬────────┐
│ الحساب                │ مدين   │ دائن   │
├────────────────────────┼────────┼────────┤
│ 113 - ذمم مدينة       │ 250    │   -    │
│ 411 - إيرادات الخدمات │   -    │ 250    │
└────────────────────────┴────────┴────────┘
```

📊 **قاعدة البيانات**:
```
هذه القيود تُخزن في:
- Supabase: operations table
- يتم إنشاءها تلقائياً في journal_entries
```

---

### 5️⃣ إنشاء عملية شراء (Purchase Operation)
**الخطوة الخامسة: شراء قطع غيار من مورد**

📍 **الصفحة**: `Operations` > عملية جديدة
- نوع العملية: `purchase` (شراء)
- المورد: اسم المورد
- البنود:
  ```javascript
  items: [
    { itemType: 'part', name: 'فلتر هواء', quantity: 5, price: 40 }
  ]
  ```
- طريقة الدفع: `cash` أو `credit`

💫 **القيد المحاسبي التلقائي (نقدي)**:
```
التاريخ: 2025-01-25
الوصف: شراء قطع غيار - مورد XYZ

┌──────────────────────────┬────────┬────────┐
│ الحساب                  │ مدين   │ دائن   │
├──────────────────────────┼────────┼────────┤
│ 514 - مصروفات قطع غيار  │ 200    │   -    │
│ 101 - النقدية           │   -    │ 200    │
└──────────────────────────┴────────┴────────┘
```

**إذا كانت آجلة**:
```
┌──────────────────────────┬────────┬────────┐
│ الحساب                  │ مدين   │ دائن   │
├──────────────────────────┼────────┼────────┤
│ 514 - مصروفات قطع غيار  │ 200    │   -    │
│ 211 - ذمم دائنة موردين  │   -    │ 200    │
└──────────────────────────┴────────┴────────┘
```

---

### 6️⃣ تحديث حالة المركبة
**الخطوة السادسة: تتبع تقدم العمل**

📍 **الصفحة**: `Dashboard` أو `VehicleDetails`
- تحديث الحالة:
  - `diagnosis` → `quotation` → `repair` → `ready` → `delivered`
- عند كل تحديث، يتم تحديث:
  - نسبة الإنجاز
  - الفني المسؤول
  - ملاحظات

⚠️ **لا يوجد قيد محاسبي عند تحديث الحالة**

---

### 7️⃣ إنشاء وإصدار الفاتورة
**الخطوة السابعة: إصدار الفاتورة للعميل**

📍 **يتم تلقائياً عند**: تغيير حالة المركبة إلى `ready` أو `delivered`

📊 **قاعدة البيانات**:
```
Table: invoices
{
  id,
  vehicle_id,
  customer_name,
  items: [...نفس البنود من المركبة...],
  subtotal: 250,
  tax: 37.5 (15%),
  total: 287.5,
  status: 'draft' → 'issued' → 'paid',
  issue_date, due_date
}
```

💫 **القيد المحاسبي عند الدفع**:
```
عندما يدفع العميل:

┌────────────────────────┬────────┬────────┐
│ الحساب                │ مدين   │ دائن   │
├────────────────────────┼────────┼────────┤
│ 101 - النقدية         │ 287.5  │   -    │
│ 113 - ذمم مدينة       │   -    │ 250    │
│ 231 - ضريبة مستحقة    │   -    │  37.5  │
└────────────────────────┴────────┴────────┘
```

---

### 8️⃣ تسليم المركبة
**الخطوة الثامنة: تسليم المركبة للعميل**

📍 **الصفحة**: `Dashboard` > Quick Actions > تسليم
- تحديث الحالة إلى: `delivered`
- تسجيل تاريخ التسليم
- إغلاق الفاتورة

📊 **التحديثات**:
- `vehicle.status = 'delivered'`
- `vehicle.completionDate = today`
- `invoice.status = 'paid'` (إذا تم الدفع)

---

## 📊 التقارير المالية التلقائية

### Chart of Accounts (دليل الحسابات)
**المسار**: `/accounting/chart-of-accounts`

يتم بناؤه تلقائياً من العمليات:
```python
# حساب الأرصدة من operations
for operation in operations:
    if operation.type == 'sale':
        if payment_method == 'cash':
            accounts['101'].balance += total  # النقدية
        else:
            accounts['113'].balance += total  # ذمم مدينة
        accounts['411'].balance += total      # إيرادات
    
    elif operation.type == 'purchase':
        accounts['514'].balance += total      # مصروفات قطع
        if payment_method == 'cash':
            accounts['101'].balance -= total  # النقدية
        else:
            accounts['211'].balance += total  # ذمم دائنة

# الأرباح المحتجزة
accounts['302'].balance = revenue - expenses
```

**الحسابات الرئيسية**:
```
الأصول (1xx):
  101 - النقدية
  113 - ذمم مدينة عملاء
  121 - مخزون قطع الغيار

الالتزامات (2xx):
  211 - ذمم دائنة موردين
  231 - ضريبة القيمة المضافة

حقوق الملكية (3xx):
  301 - رأس المال
  302 - الأرباح المحتجزة

الإيرادات (4xx):
  411 - إيرادات الخدمات

المصروفات (5xx):
  511 - الرواتب
  514 - مصروفات قطع الغيار
```

---

### Balance Sheet (الميزانية العمومية)
**المسار**: `/accounting/balance-sheet`

تُحسب من مجاميع الحسابات:
```
الأصول = النقدية + الذمم المدينة + المخزون
الالتزامات = الذمم الدائنة + الضرائب المستحقة
حقوق الملكية = رأس المال + الأرباح المحتجزة

المعادلة: الأصول = الالتزامات + حقوق الملكية
```

---

### Income Statement (قائمة الدخل)
**المسار**: `/accounting/income-statement`

تُحسب من فترة زمنية محددة:
```python
# الإيرادات (من عمليات البيع)
revenue = sum(operations where type='sale')

# المصروفات (من عمليات الشراء + رواتب)
expenses = sum(operations where type='purchase') + salaries

# صافي الدخل
net_income = revenue - expenses

# هامش الربح
profit_margin = (net_income / revenue) × 100
```

---

### Cash Flow (التدفقات النقدية)
**المسار**: `/accounting/cash-flow`

تُحسب من تحركات النقد:
```python
# الأنشطة التشغيلية
operating_cash = (sales_cash - purchases_cash - salaries)

# الأنشطة الاستثمارية
investing_cash = (equipment_sales - equipment_purchases)

# الأنشطة التمويلية
financing_cash = (new_loans - loan_payments)

# صافي التغير في النقد
net_change = operating + investing + financing

# رصيد النقد النهائي
ending_cash = beginning_cash + net_change
```

---

### Journal Entries (القيود اليومية)
**المسار**: `/accounting/journal-entries`

تُعرض القيود من مصدرين:

**1. قيود تلقائية من العمليات:**
```javascript
Source: operations table
كل عملية (sale/purchase) تُنشئ قيد تلقائياً
```

**2. قيود يدوية:**
```javascript
Source: journal_entries table
يمكن إضافة قيود يدوية لحالات خاصة:
- قيود التسوية
- قيود الإهلاك
- قيود الإقفال
```

---

## 🔄 مثال عملي كامل

### السيناريو: عميل يأتي بسيارة تويوتا كامري

#### الخطوة 1: دخول المركبة
```
Dashboard > إضافة مركبة
- Brand: تويوتا
- Model: كامري
- Year: 2020
- Plate: ABC1234
- Customer: أحمد محمد
- Status: diagnosis
```

#### الخطوة 2: التشخيص وإضافة البنود
```
VehicleDetails > إضافة بند
- تغيير زيت المحرك: 150 ريال
- فلتر زيت: 50 ريال × 2 = 100 ريال
- فحص شامل: 100 ريال

المجموع التقديري: 350 ريال ✅
```

#### الخطوة 3: إنشاء عملية بيع
```
Operations > عملية جديدة
- Type: sale
- Vehicle: تويوتا كامري ABC1234
- Items: [تغيير زيت: 150, فلتر زيت: 100, فحص: 100]
- Total: 350
- Payment Method: cash
- Customer: أحمد محمد
```

#### الخطوة 4: القيد المحاسبي التلقائي
```
📅 2025-01-25
📝 بيع خدمات - تويوتا كامري ABC1234 - أحمد محمد

من مذكورين:
  101 - النقدية                    350 ريال
إلى مذكورين:
  411 - إيرادات الخدمات            350 ريال

✅ القيد متوازن
```

#### الخطوة 5: تحديث الحسابات
```
Chart of Accounts:
  101 - النقدية: +350 ريال
  411 - إيرادات الخدمات: +350 ريال
  302 - الأرباح المحتجزة: +350 ريال
```

#### الخطوة 6: التقارير المالية
```
Balance Sheet:
  الأصول (النقدية): 350
  الالتزامات: 0
  حقوق الملكية (أرباح): 350
  ✅ متوازن: 350 = 0 + 350

Income Statement:
  الإيرادات: 350
  المصروفات: 0
  صافي الدخل: 350
  هامش الربح: 100% 🎉

Cash Flow:
  الأنشطة التشغيلية: +350
  رصيد النقد: 350
```

---

## 🛡️ نظام التدقيق المحاسبي

### المسار: `/system-audit`

**ماذا يفحص؟**

1. **معادلة المحاسبة:**
   ```
   ✅ الأصول = الالتزامات + حقوق الملكية
   ```

2. **اتساق القوائم:**
   ```
   ✅ صافي الدخل في Income Statement
       يجب أن يساوي
       التغير في الأرباح المحتجزة في Balance Sheet
   ```

3. **الأنماط غير العادية:**
   ```
   ⚠️ هامش ربح > 95% → مصروفات ناقصة؟
   ⚠️ مصروفات < 1% من الإيرادات → غير واقعي
   ⚠️ 90%+ عمليات نقدية → عمليات آجلة مفقودة؟
   ```

4. **التوازن:**
   ```
   ✅ كل قيد: المدين = الدائن
   ```

**النتيجة:**
- درجة صحة النظام: 0-100
- قائمة بالمشكلات
- التصحيحات المطلوبة
- التوصيات

---

## 🗑️ إعادة تعيين البيانات

### المسار: `Chart of Accounts` > زر "إعادة تعيين الكل"

**ماذا يحذف؟**
```
✅ جميع الحسابات (chart_of_accounts)
✅ جميع العمليات (operations)
✅ جميع القيود اليومية (journal_entries)
```

**التأكيد:**
1. Confirm: "هل أنت متأكد؟"
2. Prompt: "اكتب: حذف كل شيء"

**النتيجة:**
- النظام يعود للصفر
- يمكن البدء من جديد

---

## 📈 ملخص تدفق البيانات

```
دخول مركبة
    ↓
تشخيص وإضافة بنود
    ↓
حساب المبلغ التقديري تلقائياً
    ↓
إنشاء عملية (sale/purchase)
    ↓
🔄 قيد محاسبي تلقائي
    ↓
تحديث دليل الحسابات
    ↓
إنشاء التقارير المالية تلقائياً
    ↓
🛡️ تدقيق النظام
```

---

## 🎯 النقاط الرئيسية:

1. **القيود تُنشأ تلقائياً** من العمليات (operations)
2. **الحسابات تُحدث تلقائياً** من القيود
3. **التقارير تُبنى تلقائياً** من الحسابات
4. **التدقيق يكتشف المشاكل** تلقائياً
5. **يمكن البدء من الصفر** في أي وقت

**النظام متكامل ومترابط! 🚀**
